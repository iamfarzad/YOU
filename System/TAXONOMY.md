# Taxonomy (Hidden Scaffolding)

This vault contains a broad ontology (including the legacy 51 categories), but those are latent slots.
Only categories with enough evidence should appear in `Visible/`.
